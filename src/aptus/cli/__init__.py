"""Thin CLI entrypoints registered as console scripts in pyproject.toml.

Each module exposes a ``main(argv) -> int`` so it is both a console-script target
and unit-testable. See docs/07_engineering_standards.md section 2.
"""
